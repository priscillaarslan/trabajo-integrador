// paso 1, capturar id rn querystring 
/* 
let queryString = location.search;
let queryStringObj = new URLSearchParams(queryString);
let id = queryStringObj.get("id");
console.log(id);
*/
// paso 2, hacert fetch a endpoint de artista con id
// paso 3, capturaR seccion del html 
// paso 4, ponerle el html a section con la info del endpoint
